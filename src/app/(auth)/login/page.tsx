import { LoginScreen } from "@/components/auth/login-form";

/** The web sign-in. The screen itself is shared with /desktop-login — see components/auth/login-form. */
export default function LoginPage() {
  return <LoginScreen />;
}
