# UI Localization (i18n) — English / Vietnamese / Japanese

## Purpose

WarpTalk's UI ships in three display languages — English (`en`), Vietnamese (`vi`), Japanese (`ja`) — chosen by the person using the app, independent of the meeting/transcription language they speak. Ticket: WT-607.

This is a **from-scratch i18n layer**: before this, `warptalk-web` had none (see `scripts/check-english-ui.mjs`'s original doc comment), and the shipped UI was English-only by explicit CI-enforced policy. This doc is the "how do I add a new translatable string" reference the ticket's acceptance criteria asked for.

**Do not confuse this with `src/lib/language/languages.ts`.** That file is the meeting/transcription language registry (source/target language for live translation, room language policy) — a completely separate concern from the UI chrome text this doc covers. "Locale" in this doc always means UI display language; "language" elsewhere in the codebase usually means meeting content language.

## Architecture

- **Library**: [`next-intl`](https://next-intl.dev), the standard for Next.js App Router.
- **No URL locale prefix.** `warptalk-web`'s routes already use `(app)/[workspaceSlug]/...` as the first dynamic segment under 115+ pages. Rather than restructure every route under `app/[locale]/...` (a large, risky migration touching every internal link, invite link, and email redirect), locale is resolved **without changing the URL**: a `WARPTALK_LOCALE` cookie (falling back to the browser's `Accept-Language`, then `en`). See `src/i18n/locale.ts` (`getUserLocale`) and `src/i18n/actions.ts` (`setUserLocale`, a server action that writes the cookie).
- **Request config**: `src/i18n/request.ts` — `next-intl`'s `getRequestConfig`, resolves the locale and loads/merges that locale's message namespace files.
- **Provider wiring**: `src/app/layout.tsx` is an async Server Component; it reads the resolved locale (`getLocale()`), sets `<html lang={locale}>`, and wraps the existing client `<Providers>` tree in `<NextIntlClientProvider>`.
- **`next.config.ts`** is wrapped with `createNextIntlPlugin("./src/i18n/request.ts")`.
- **Message catalogs** live at the repo root in `messages/{en,vi,ja}/*.json` — **outside** `src/`. This is deliberate: `scripts/check-english-ui.mjs` scans only `src/**/*.{ts,tsx}` for non-English characters, so keeping catalogs outside `src/` means that guard keeps doing useful work — it still blocks anyone from pasting raw Vietnamese/Japanese text directly into a component instead of going through the catalog.
- **Switching locale**: `src/components/layout/language-switcher.tsx` — a dropdown that calls the `setUserLocale` server action, then `router.refresh()`. Takes an optional `compact` prop (icon-only, `sr-only` label, sized to match the other 24px circular topbar controls like `ThemeToggleButton`) for tight chrome. Wired into the landing navbar, all five auth pages (via `CinematicAuthShell` for forgot/reset/verify, and directly on login/register), and the authenticated app shell's topbar (`src/app/(app)/layout.tsx`, `compact`).

## How to add a new translatable string

1. **Pick (or create) a namespace file.** Namespaces mirror feature areas: `common` (shared chrome — buttons, nav, toasts used everywhere), `auth`, `landing`, `legal`, `validation`. A new feature area gets its own file, added to `NAMESPACES` in `src/i18n/request.ts` and created as `messages/{en,vi,ja}/<namespace>.json`.
2. **Add the English key first** (`messages/en/<namespace>.json` is the source of truth) — nested objects are fine, e.g. `"toasts": { "saveFailed": "..." }` is read as `t("toasts.saveFailed")`.
3. **Add the same key to `messages/vi/<namespace>.json` and `messages/ja/<namespace>.json`.** Translate for meaning and tone, not word-for-word — Vietnamese and Japanese have different sentence structure, and a literal translation usually reads as broken. Use `{placeholder}` for interpolated values (e.g. `"Switched to workspace \"{name}\""` — ICU-style, resolved via `t("key", { name })`).
4. **Read it in the component:**
   - Client Component: `const t = useTranslations("landing"); ... {t("hero.title")}`
   - Server Component: `const t = await getTranslations("legal.terms"); ... {t("title")}`
5. **Zod validation schemas** can't call `useTranslations()` at module scope (it's a hook). Pattern used throughout the auth pages: turn the schema into a factory function that takes a translator, e.g. `getLoginSchema(tv: ReturnType<typeof useTranslations>)`, and build it inside the component with `useMemo(() => getLoginSchema(tv), [tv])` where `tv = useTranslations("validation")`.
6. **Plain (non-component) helper functions** that produce user-facing text (e.g. `src/lib/utils.ts`'s `getPlanDescription`/`buildFeatureList`) take an **optional** translator parameter defaulted to the pre-migration English strings, so call sites that haven't been migrated yet keep compiling and keep their existing copy — see that file for the exact pattern.
7. **Run `npm run test:i18n-catalog`** before committing — it fails loudly if a key exists in `en` but is missing (or renamed) in `vi`/`ja`, so a forgotten translation can't silently fall back to English in production. It's wired into `npm run test:contracts`.
8. **Never hardcode a raw Vietnamese/Japanese string directly in a `.tsx`/`.ts` file under `src/`** — `npm run test:english-ui` will fail the build. If you genuinely need literal non-English data (e.g. a language-name table), mark the block with an `i18n-allow` comment, same convention as `src/lib/language/languages.ts`.

## Deliberately not translated (documented, not accidental)

A few spots keep English-only text on purpose, called out inline in the code with a comment:

- **`src/app/page.tsx`**: the hand-tuned SVG story-board branch labels (`live`, `low latency`, `room signal`, crossing-diagram language names, `decisions`/`questions`/etc.) and the small `feature-wave-labels`/`feature-language-line` word chips. These are positioned/sized against exact English character widths in a bespoke animation; translating them needs per-locale layout re-tuning, not a blind string swap.
- **`src/app/page.tsx`**'s `LoadingScreen` splash (`loaderWords`, the `WalpTalk` wordmark) — transient branding, not information-bearing.

## What's migrated (Phase A + Phase B batches) vs. what's next

Per WT-607's own scope ("không cần dịch toàn bộ ứng dụng trong ticket đầu tiên này nếu blast radius quá lớn"), this rollout is a sequence of small, independently-verified batches (typecheck + lint + `test:i18n-catalog` + `test:contracts` + build, then a commit, per batch) rather than a full-app pass in one PR. Batch order and the layout-safety strategy are in the plan history; this section tracks actual progress.

**Done — Phase A (landing/auth/legal/nav):**
- Landing page (`src/app/page.tsx`) — nav, hero, feature/signal copy, pricing chrome, footer.
- All five auth pages — login, register (+ zod schemas), forgot-password, reset-password, verify-email — and `CinematicAuthShell`/`LegalPlaceholder` shared components.
- `terms` / `privacy` pages.
- The primary authenticated app nav in `linear-sidebar.tsx` (`mainNav`, `workspaceNav`, the workspace-switch toasts) and the topbar's `compact` `LanguageSwitcher`.

**Done — Phase B batch 1 (core workspace, light pages):**
- `home`, `tasks`, `ai-chat` pages — fully self-contained, no notable subcomponents.
- `dashboard` page **and its five subcomponents** (`dashboard-hero`, `cycle-summary`, `member-usage`, `usage-breakdown`, `usage-trend`) — including proper ICU plural forms (`{count, plural, one {...} other {...}}`) for "N day(s)"/"N document(s)"/"N charge(s)", which Phase A's simpler `{count}`-substitution pattern didn't need but this batch's English source text actually required for correctness.
- `knowledge` page **and its two subcomponents** (`knowledge-table`, `knowledge-chunk-sheet`) — including the shared lib `src/lib/knowledge/knowledge-view.ts` (`sourceLabel`, new `sourceTypeLabel`, `translatedSourceTabs`, all with the optional-translator pattern) and `src/lib/billing/usage-labels.ts` (`usageTypeLabel`/`usageTypeDetailLabel`, same pattern) — both **shared with not-yet-migrated pages** (the admin Knowledge tab, the billing pages), so those keep compiling and keep today's English via the same default-parameter trick as `getPlanDescription`.
- `voice-profiles` page **and all five of its subcomponents** (`voice-consent-card`, `my-dub-voice-picker`, `library-voice-list`, `voice-profile-list`, `create-voice-profile-dialog`).

**A hard exception, found and deliberately preserved — not a gap to fix later:** the five consent statement labels in `create-voice-profile-dialog.tsx` (`CONSENT_ITEMS`) are **never routed through i18n**. The server hashes that exact English text (`VoiceProfileConsentContract.CanonicalContractText`) and the hash is the only record of what a person agreed to — translating the display text would desynchronize the on-screen wording from what the hash actually attests to. Only the chrome around them (dialog title, buttons, other fields) is translated. See the comment at that file's `CONSENT_ITEMS` declaration before ever touching it.

**A `isKnownFactCategory` lesson worth repeating**: it was first added to `src/lib/knowledge/knowledge-view.ts`, which has a **hard contract-test-enforced constraint** (`check-admin-knowledge-contract.mjs`) requiring that file to stay free of *value* imports (type-only), so `node:test` can exercise it without a module resolver. Importing `FACT_CATEGORIES` as a value there broke that contract. Fixed by moving the helper to `src/types/workspace-knowledge.ts`, which already owns `FACT_CATEGORIES` as a value export. **Before adding a helper to a `lib/*-view.ts` file, check whether a contract script asserts that file's import shape.**

**Contract tests updated to stay meaningful, not weakened:** `scripts/check-admin-knowledge-contract.mjs` used to `assert.match` the page/component *source* against literal English strings ("Only a workspace Owner or Admin can see what has been indexed", "Could not read the index", "Nothing indexed yet") to prove a gate/state existed. Once those strings became `t("key")` calls, the literal-text assertions started failing — correctly, since the literal text really was gone from the source. Fixed by asserting **both halves of the same guarantee**: the component source calls the expected translation key, AND `messages/en/<namespace>.json` still carries the original English wording at that key. This is the general fix pattern for any future contract test that breaks the same way — never delete the assertion, split it in two.

**Done — Phase B batch 8 (the live meeting room, in full except one component):** the ~2,940-line room detail route (`rooms/[id]/page.tsx`, fully client-side — the whole file is `"use client"`, not a server shell) and its two supporting lib modules, `src/lib/meeting/record-sharing.ts` (`describeRecordSharing`, now takes an optional `t` defaulted to the pinned English strings its own `node:test` suite asserts) and `src/lib/meeting/room-occupancy.ts` (`PRESENCE_LABELS` stays the untranslated pinned constant; added `presenceLabel(presence, t?)` for translated call sites — no current caller yet, but the infrastructure now exists for `people-panel.tsx` or a future migration to use) — namespace `messages/{en,vi,ja}/meetingRoomPage.json`. Alongside it, in the same session, four sibling areas of the live meeting UI were migrated in parallel: the bottom toolbar and its settings menu (`src/components/rooms/live/meeting-control-bar.tsx` + `mic-check.tsx`, namespace `meetingControlBar`, 77 keys — noise suppression/mic filter/background blur/layout/voice/transcript pause/flash-mode/room-lock/mute controls, plus the language-pair picker and the mic-check widget's background-noise copy), the call chrome (`meeting-ready-card.tsx`, `live-subtitle-overlay.tsx`, `meeting-stage.tsx`, namespace `meetingCallChrome` — the ready-card share prompt, the live captions lane including its history view, and the stage's camera-off/presenting/connection-state badges), the chat/WarpBot side panel (`chat-panel.tsx`, namespace `meetingChat` — composer, errors, the WarpBot-queue ICU plural, upload progress, history states), the saved Transcript tab (`meeting-transcript-panel.tsx` + `src/lib/meeting/transcript-absence.ts`, namespace `meetingTranscript` — toolbar chips with a real ICU `plural`+`select`, every toast, the version-history popover, both correction editors; `transcript-absence.ts`'s optional-`t` default needed a runtime `typeof` guard rather than a plain default parameter, because its test calls it bare via `.map()`, which passes the array index as the second argument), and the record's Summary/Talk-time rail (`meeting-reading-rail.tsx` + `src/lib/meeting/summary-absence.ts`, namespace `meetingSummary` — including the previously under-inventoried `RailTalkTime` per-speaker percentages). The only component in this area still untouched is `src/components/rooms/feedback-menu.tsx` (`MeetingFeedbackMenu`) — out of scope for this batch, not yet migrated.

**Contract-script breaks from this batch — all repaired centrally, in the same pass, using the split-assertion pattern** (source now calls the translation key + the English catalog still carries the original wording): `check-room-surface-contract.mjs` (two assertions: the `Participants: {label}` panel line and the People panel's `title="People"`), `check-room-detail-thread-flow.mjs` ("Room notes" heading), `check-meeting-camera-off-ui-contract.mjs` ("Camera is off"), `check-meeting-chat-contract.mjs` ("Could not load chat history"), `check-caption-toggle-honesty.mjs` (rewritten to read the CC label's wording from `meetingControlBar.json` instead of regexing a literal ternary out of source, since the ternary now calls `t(...)`), `check-mic-noise-filter-contract.mjs` ("Mic noise filter" label, plus a check that it stays worded differently from "Noise suppression"), `check-recording-control-contract.mjs` ("Recording request in progress"), `check-copy-room-code-contract.mjs` ("Copy room code" label + "Room code" toast), `check-reading-rail-contract.mjs` and `check-traceable-narrative-contract.mjs` ("no moment recorded", "Click a sentence to see where it came from."), `check-wt-225-230-contract.mjs` ("Save correction"), and `check-summary-anchor-group-contract.mjs` (the "not in the saved transcript" toast). None were weakened — every one still pins the original English wording, just against the catalog file rather than the component source. `check-recording-seek-contract.mjs`'s `/Follow playback/` match still passes, but only because a doc comment above `FollowPlaybackChip` happens to still contain that substring even though the JSX itself now calls `t("followPlayback.label")` — left as-is since it isn't actually broken, but worth tightening to match JSX rather than a comment if it's ever touched again.

- The `occupancyNoun` pill contract (`check-room-surface-contract.mjs:471-475`, requiring the literal substring `attended` near `occupancyNoun={`) was kept passing on purpose, with no fix needed: the translation keys are named `t("occupancyNoun.attended")` / `t("occupancyNoun.inRoom")`, so the pinned substring is still present in source even though it is now a catalog key rather than the rendered value.

**Done — admin console, System Admin Insights dashboard (extends the existing `adminOps` namespace, does not create a new one):** `src/components/admin/insights/insights-dashboard.tsx` and `src/lib/admin/insights-metrics.ts` — 170 new keys added as siblings under `adminOps.insights.*`, alongside the pre-existing `aiCostBasis`/`cartesia` subtrees (left untouched). `insights-metrics.ts` was restructured to match the data/wording split those two subtrees already modeled: every wording function (`deltaText`, `mrrSub`, `churnSub`, `outstandingSub`, `revenueTodaySub`, `workspaceLabel`, `periodCardView`, `assembleNeedsAttention`, etc.) now takes an optional `t?: InsightsTranslator` last parameter via a shared `say(t, key, fallback, values?)` helper, following the `getPlanDescription` optional-translator pattern — `src/lib/admin/__tests__/insights-metrics.test.ts` (28 tests, pinning exact English wording, not just numbers) passes unmodified. `PERIOD_CARDS`/`METRIC_LABELS` and the CSV export (`insightsCsv`/`insightsCsvRows`) were deliberately left English-only: the CSV is a downloaded file, not on-screen chrome, and its own tests pin literal English. `insights-charts.tsx` needed no changes — it only ever rendered props, no hardcoded strings. Not in scope, flagged rather than silently dropped: `period.label`/`period.caption` (e.g. "Today", "Last 7 days") come from `src/lib/admin/insights-period.ts`, outside this task's file list, and still render in English; the server-supplied `note` caption strings on metric DTOs (e.g. "Started paying...") are backend content, not present anywhere in `src/`, and are out of frontend-i18n scope entirely.
- One contract break, repaired centrally: `check-admin-portal-contract.mjs`'s `NOT_AVAILABLE_NOTE` identifier match — the dashboard now renders `t("common.notAvailable")` instead of importing the constant directly (the constant still exists in `insights-metrics.ts` as the untranslated default). Fixed with the same split-assertion pattern: the dashboard calls the key, and `adminOps.insights.common.notAvailable` in the English catalog still reads "Not available yet".

**Done — `src/components/admin/AdjustCreditModal.tsx` (the platform billing page's "Adjust Credits" dialog):** the one remaining untranslated piece of `/admin/billing` (and its legacy twin `/billing`, both flagged in a user screenshot showing the rest of the page in Vietnamese but this dialog still in English). It is a standalone shared component (also used from `/admin/workspaces/[workspaceRef]`), so it owns its own scoped translator — `useTranslations("adminBillingLedger.adjustCredits")` — rather than inheriting whatever namespace its host page happens to use. Extends the existing `adminBillingLedger` namespace with a new `adjustCredits` key (trigger, title, description with/without a pinned target workspace, the confirmation step, every form field/placeholder/hint, and every validation/success/error message). No contract script referenced this component's literal strings, so nothing needed repairing.

**`2026-09-24`: the create-room meeting-type picker, and the same type name shown read-only on a room's own page.** User-reported gap: the "Loại cuộc họp" dropdown header was already translated (part of the `rooms.create.templatePicker` namespace from an earlier pass) but its seven option labels — Event, Channel Meeting, Webinar, Company Meeting, Virtual Appointment, Live Event, External Meeting — were still hardcoded English `label` fields on `MEETING_TYPES` in `src/lib/meeting/meeting-types.ts`, rendered directly by `src/components/rooms/create/template-picker.tsx`.

Translating them exposed a pre-existing design issue: `create-room-dialog.tsx`'s `meetingTemplate` state held the meeting type's **label text itself** ("Event") as its identity, looked up via `meetingTypeByLabel(meetingTemplate)` — the picker's display string doubled as its own lookup key. Once the picker started showing a translated label, that key would have silently broken (switching locale, or selecting the Vietnamese-labelled option, would stop matching any entry, always falling back to the first type). Fixed by decoupling identity from display: `meetingTemplate` now holds the type's stable `value` (e.g. `"EVENT"`), resolved via `meetingTypeByValue(meetingTemplate) ?? MEETING_TYPES[0]` (same fallback shape `meetingTypeByLabel` used); the picker's `onChange` now emits `type.value` instead of `type.label`. `meeting-types.ts` gained a small exported map, `MEETING_TYPE_I18N_KEYS: Record<string, string>` (value → camelCase message key), so any surface displaying a type's name resolves it through `t(\`types.${MEETING_TYPE_I18N_KEYS[value]}\`)` rather than reading `.label` (which still exists on `MeetingType`, unchanged, as the fixed-English string `meetingTypeByLabel` still matches against — not UI text). The read-only meeting-type chip on `rooms/[id]/MeetingPropertiesPills.tsx` (previously plain `{meetingType.label}`, always English) was translated the same way for consistency, since it shows the identical data. New keys added under the existing `rooms.create.templatePicker.types.*` (7 keys × 3 locales).
- **Contract fix:** `scripts/check-room-surface-contract.mjs` asserted the literal expression `requiresApproval ?? meetingTypeByLabel(meetingTemplate).defaults.requiresApproval`, which the refactor above legitimately changed. Repaired with two assertions instead of weakening one: that `selectedMeetingType` is resolved via `meetingTypeByValue(meetingTemplate) ?? MEETING_TYPES[0]`, and that the approval toggle still reads `requiresApproval ?? selectedMeetingType.defaults.requiresApproval` — same "explicit choice → meeting type default" rule the original assertion existed to protect, expressed through the new variable.

**`2026-09-24`: `src/app/(internal)/billing/plans/page.tsx` (`AdminPlansPage`) — the legacy plan-management screen, mounted at both `/billing/plans` and `/admin/billing/plans`.** User-reported: fully English despite the surrounding `/admin/billing` shell already being translated (this page predates the newer, already-translated `/admin/plans`/`pricingEditors` editor and was deliberately kept rather than redirected — see the app-layout doc's basePath note). New dedicated namespace `adminBillingPlansLegacy` (registered in `src/i18n/request.ts`), **not** a reuse/extension of `adminPlansSettings.pricingEditors` — that namespace's field wording ("Name", "Slug", "Credits per cycle"...) differs from this page's own English source ("Plan Name", "Slug (URL identity)", "Credits/Cycle"...), and coupling them would either change this page's visible English text or make the two pages drift out of sync if one's copy is edited later without the other in mind. Covers: the header (badge/title/description/"Add New Plan"), the package table (columns, the "Free" price fallback, "Voice Clone: {status}"/"Glossary Access: {status}" via ICU interpolation against shared `enabled`/`disabled` keys, "Active"/"Inactive"), the full create/edit dialog (title branching, every field label/placeholder, the currency/billing-cycle `Select` options, the Feature Authorization Flags section, sort order, plan-highlights textarea, active-status toggle, footer buttons), and every `handleSave` validation message plus both mutation `onError` fallbacks (interpolated: `errors.priceTooLow` takes `{currency, minPrice}`). Currency codes (`VND`/`USD`) and billing-cycle data values (`monthly`/`semiannual`/`yearly`) are left as literals — they are wire values, not display copy.
- No contract script asserted this page's literal strings (checked: `grep` across `scripts/` for the page's distinctive copy and the `AdminPlansPage` identifier — only `check-admin-surface-contract.mjs`'s path-based nav-exemption list references this file, unaffected since it matches by file path, not content).

**Not yet migrated — follow the pattern above, page by page:**
- The ~90 remaining authenticated app pages per the batch plan: rooms/history/schedules (batch 2, remainder), documents/glossary/members (batch 3), settings/billing (batch 4), the rest of the admin console — 12 more pages beyond Insights (batch 5), platform billing/workspace lifecycle (batch 6), standalone routes (batch 7).
- `src/components/rooms/feedback-menu.tsx` (`MeetingFeedbackMenu`) — the one component left in the live meeting room area (batch 8, remainder).
- The admin console nav and the settings-page collapsed nav inside `linear-sidebar.tsx` (same file as the migrated nav — deliberately scoped out, covered by batch 5).
- The remaining `toast(...)` call sites outside the migrated files.
- Email templates under `src/emails` (React Email renders server-side without the request-scoped locale cookie readily available; needs the recipient's saved locale preference passed explicitly at send time).
- Persisting a user's locale preference server-side (`warptalk-backend`) so it follows them across devices — today it's a browser cookie only, which is enough for a working feature but not cross-device.

**`2026-09-24`: `src/components/rooms/meeting-reading-rail.tsx` + `src/lib/meeting/summary-absence.ts` (batch 2, rooms) — the meeting record's Summary/Talk-time rail.** New namespace `meetingSummary` (already registered in `src/i18n/request.ts`) at `messages/{en,vi,ja}/meetingSummary.json`. Every hardcoded string in the rail — the recording pip label/toggle, the tab bar (`aria-label`, "Summary"/"Talk time"), the copy/download toasts and titles, the four empty-state headings, the shape/language pickers ("As spoken", `aria-label`s, titles), the rendering-version notice, the `copyAsText()` clipboard text (title line, "(no overview)", "(none recorded)"), the narrative hint and per-sentence/per-claim jump titles, "no moment recorded", the uncited-count footnote (now a real ICU `plural`), and the whole of `RailTalkTime` (empty state, "Share of the talking" heading, the per-speaker percent `aria-label` via `{percent}%` interpolation — already an integer, and the footnote) — now read through `useTranslations("meetingSummary")`, one hook call per function component in the file (`ReadingRail`, `RailSummary`, `RailClaimButton`, `RailNarrativeSentence`, `RailTalkTime`). `summary-absence.ts`'s `summaryAbsenceMessage(absence, t?)` follows the optional-translator-parameter pattern from `getPlanDescription` in `src/lib/utils.ts`: `t` defaults to a function returning today's exact English strings keyed by `absence.<camelCase>`, so `src/lib/meeting/__tests__/summary-absence.test.ts`'s literal-English `assert.match`es keep passing unmodified; the rail now calls it as `summaryAbsenceMessage(absence, t)`, passing its own `meetingSummary`-scoped translator straight through.
  - **Broke two literal-text contract assertions — since repaired centrally** (split into a source-calls-the-key half and an en-catalog-still-has-the-wording half, per the pattern above): `scripts/check-reading-rail-contract.mjs`'s `no moment recorded` match now reads `t("claim.noMoment")` from source plus `meetingSummary.json`'s `claim.noMoment`; `scripts/check-traceable-narrative-contract.mjs`'s two matches (the same `claim.atMs === null ? ... : formatCitationTime(...)` ternary, and `Click a sentence to see where it came from.`) now read `t("claim.noMoment")`/`t("narrative.hint")` from source plus the catalog.
  - `scripts/check-summary-state-wired.mjs`, `scripts/check-meeting-record-contract.mjs`, `scripts/check-recording-seek-contract.mjs` — re-run clean; they pin identifiers/structure/layout classes, not this batch's translated copy.

**`2026-09-24`: `src/components/rooms/meeting-transcript-panel.tsx` + `src/lib/meeting/transcript-absence.ts` (batch 2, rooms) — the meeting record's saved Transcript tab.** Reuses the already-registered `meetingTranscript` namespace at `messages/{en,vi,ja}/meetingTranscript.json`. Every hardcoded string in the panel — the toolbar chips (`Saved`/`Live` · entry count, now a real ICU `plural` combined with an ICU `select` on `isEnded`; Duration; Translation started; Finalized & locked, with their titles), the Copy/Download/Discard/Edit-all/Done-&-save-all/Finalize-&-lock buttons and the finalize confirm dialog, every toast (`correctionSaved`, `correctionSaveFailed`, `finalizeSuccess`, `finalizeFailed`, the batch-save toasts — two with real ICU plurals for the line/correction counts), the session divider ("Translation N" + the "now" fallback), `TranscriptLanguageStatus`'s four states (all with correct ICU plurals on the missing/entry counts, matching the original per-branch singular/plural wording exactly), the language picker ("As spoken", "Read this transcript in", the per-option detail line), the layout toggle labels, "You"/"Unknown speaker", every `aria-label`/`title` on the per-line edit pencil and the seek timestamp, the find bar, the version-history popover and list, and both correction editors (batch + single-line) — now read through `useTranslations("meetingTranscript")`, one hook call per function component in the file. `transcript-absence.ts`'s `transcriptAbsenceMessage(absence, t?)` follows the optional-translator-parameter pattern, with one wrinkle worth reading before touching it again: `src/lib/meeting/__tests__/transcript-absence.test.ts` calls it bare as `states.map(transcriptAbsenceMessage)`, which passes the array **index** as the second argument at runtime regardless of what TypeScript's overloads say the type is — a plain default parameter never sees `undefined` there and would crash on a number. The fix is a `typeof maybeT === "function"` runtime guard around the default, not just a type-level default; the test keeps passing unmodified, and the panel's own call site passes a real `meetingTranscript`-scoped translator through.
  - **Broke one literal-text contract assertion — since repaired centrally**: `scripts/check-wt-225-230-contract.mjs`'s "WT-228 transcript review exposes editing and finalization actions" check now looks for `t("editor.save")` in source plus `meetingTranscript.json`'s `editor.save === "Save correction"`, instead of the bare literal.
  - `scripts/check-recording-seek-contract.mjs`'s `assert.match(transcript, /Follow playback/, …)` still passes — coincidentally, not because the string survived: the literal text is now only in a doc comment (`WT-655(C3) — "Follow playback", after the reader has scrolled away from it.`) a few hundred lines above `FollowPlaybackChip`, which now renders `t("followPlayback.label")`. Worth tightening to match JSX rather than a comment, if it's ever touched again — left alone since it isn't actually broken.
  - `scripts/check-meeting-record-contract.mjs`, `scripts/check-transcript-language-contract.mjs`, `scripts/check-transcript-pause-wired.mjs`, `scripts/check-transcript-speaker-contract.mjs` — re-run clean; they pin identifiers/structure/CSS classes, not this batch's translated copy.

## Files Affected

- `src/i18n/locale.ts`, `src/i18n/actions.ts`, `src/i18n/request.ts`, `src/i18n/catalog-completeness.ts`, `src/i18n/__tests__/catalog-completeness.test.ts`
- `messages/{en,vi,ja}/{common,auth,landing,legal,validation}.json`
- `src/components/layout/language-switcher.tsx`
- `src/app/layout.tsx`, `next.config.ts`, `package.json`
- `src/app/page.tsx`, `src/lib/utils.ts`
- `src/app/(auth)/{login,register,forgot-password,reset-password,verify-email}/page.tsx`, `src/components/auth/cinematic-auth-shell.tsx`
- `src/app/terms/page.tsx`, `src/app/privacy/page.tsx`, `src/components/legal/legal-placeholder.tsx`
- `src/components/layout/linear-sidebar.tsx`
- `scripts/check-english-ui.mjs` (doc comment only — logic unchanged)
- **Phase B batch 1**: `src/app/(app)/[workspaceSlug]/{home,tasks,ai-chat,dashboard,knowledge,voice-profiles}/page.tsx`; `src/app/(app)/[workspaceSlug]/dashboard/components/*.tsx`; `src/components/knowledge/{knowledge-table,knowledge-chunk-sheet}.tsx`; `src/components/voice/{voice-consent-card,my-dub-voice-picker,library-voice-list,voice-profile-list,create-voice-profile-dialog}.tsx`; `src/lib/knowledge/knowledge-view.ts`; `src/lib/billing/usage-labels.ts`; `src/types/workspace-knowledge.ts`; `messages/{en,vi,ja}/{home,tasks,aiChat,dashboard,knowledge,voiceProfiles}.json`; `src/i18n/request.ts` (namespaces registered); `scripts/check-admin-knowledge-contract.mjs` (assertions updated to the i18n-aware pattern above).
- **Phase B batch 8** (live meeting room, all but `feedback-menu.tsx`): `src/app/(app)/[workspaceSlug]/rooms/[id]/page.tsx`; `src/lib/meeting/{record-sharing,room-occupancy,transcript-absence,summary-absence}.ts`; `src/components/rooms/{meeting-transcript-panel,meeting-reading-rail}.tsx`; `src/components/rooms/live/{meeting-control-bar,mic-check,meeting-ready-card,live-subtitle-overlay,meeting-stage,chat-panel}.tsx`; `messages/{en,vi,ja}/{meetingRoomPage,meetingControlBar,meetingCallChrome,meetingChat,meetingTranscript,meetingSummary}.json`; `scripts/{check-room-surface-contract,check-room-detail-thread-flow,check-meeting-camera-off-ui-contract,check-meeting-chat-contract,check-caption-toggle-honesty,check-mic-noise-filter-contract,check-recording-control-contract,check-copy-room-code-contract,check-reading-rail-contract,check-traceable-narrative-contract,check-wt-225-230-contract,check-summary-anchor-group-contract}.mjs` (assertions updated to the i18n-aware split pattern).
- **Admin console, Insights dashboard**: `src/components/admin/insights/insights-dashboard.tsx`; `src/lib/admin/insights-metrics.ts`; `messages/{en,vi,ja}/adminOps.json` (extended, `aiCostBasis`/`cartesia` untouched); `scripts/check-admin-portal-contract.mjs` (`NOT_AVAILABLE_NOTE` assertion updated to the i18n-aware split pattern).
- **Admin console, Adjust Credits dialog**: `src/components/admin/AdjustCreditModal.tsx`; `messages/{en,vi,ja}/adminBillingLedger.json` (extended with a new `adjustCredits` key).
- **Meeting-type picker + read-only chip**: `src/lib/meeting/meeting-types.ts` (new `MEETING_TYPE_I18N_KEYS` map); `src/components/rooms/create/template-picker.tsx`; `src/components/rooms/create-room-dialog.tsx` (`meetingTemplate` state switched from label-keyed to value-keyed); `src/app/(app)/[workspaceSlug]/rooms/[id]/MeetingPropertiesPills.tsx`; `messages/{en,vi,ja}/rooms.json` (extended `templatePicker.types`); `scripts/check-room-surface-contract.mjs` (approval-toggle assertion updated to the new `selectedMeetingType` variable).
- **Legacy admin plans page**: `src/app/(internal)/billing/plans/page.tsx`; `messages/{en,vi,ja}/adminBillingPlansLegacy.json` (new namespace); `src/i18n/request.ts` (namespace registered).

## Typography note — CJK needs different metrics

The landing hero (`src/app/page.tsx`) sets its `<h1>` leading/tracking **per locale**:

```
locale === "ja" ? "leading-[1.14] tracking-[-0.02em]" : "leading-[0.92] tracking-[-0.065em]"
```

This is not cosmetic fine-tuning — it fixes a real defect found during browser verification. `leading-[0.92]` builds a line box *shorter than the font size*, which only works because Latin glyphs leave slack above the cap height. A CJK glyph fills its full em box, so at 0.92 the two lines of the Japanese headline physically overlapped on screen. The negative letter-spacing is relaxed for the same reason: `-0.065em` is tuned for Latin sidebearings and visibly crowds kana/kanji.

**Watch for this whenever you translate a display-size heading**: any `leading-` below ~1.0 or aggressive negative `tracking-` will break in Japanese. Latin-tuned metrics are not locale-neutral. Verify in the browser, not just in the catalog.

## Known Limitations

- No per-locale URLs — a `vi`/`ja` reader and an `en` reader see the same URL for the same page. Acceptable since these routes are either behind auth or single-instance marketing pages with no per-locale SEO requirement today.
- `<title>`/`<meta description>` metadata (`export const metadata` in page files) is still static English — `generateMetadata` per-locale is a follow-up if SEO/tab-title localization is wanted. Confirmed in-browser: `/terms` renders fully translated body copy while its tab title stays "Terms of use | WarpTalk".
- Locale preference is a browser cookie, not tied to the user's account — clearing cookies or switching browsers resets it to `en`/`Accept-Language`.
- **A validation error already on screen keeps its old-locale text until validation re-runs.** react-hook-form stores the *resolved string* in `formState.errors`, so switching locale while an error is visible leaves the previous language's message in place; it corrects itself on the next submit/validate. Verified as recoverable, not stuck. Only worth fixing (by storing keys and resolving at render) if it turns out to bother real users.
- Long-form legal content (terms/privacy) still only has the same short English-authored placeholder summary translated into vi/ja — actual legal copy, whenever written, needs its own translation pass.

## Testing Checklist

- [x] `npm run typecheck` passes; `npm run lint` reports 0 errors (43 pre-existing warnings, none in i18n-touched files).
- [x] `npm run test:i18n-catalog` passes (vi/ja key parity against en — 11 subtests).
- [x] `npm run test:english-ui` passes (no raw non-English literals leaked into `src/`).
- [x] `npm run test:contracts` passes end-to-end (exit 0, no failures) — confirms none of the ~155 existing structural/text contract checks broke on the touched files.
- [x] `npm run build` succeeds (Turbopack production build, 84 routes).
- [x] Manual: `/` switched en → vi → ja via the switcher; copy updates and **persists across navigation** (cookie is read server-side, so SSR output is already in the chosen locale).
- [x] Manual: `/login` — UI copy and **zod validation messages** translate (`Địa chỉ email không hợp lệ` / `メールアドレスの形式が正しくありません`), confirming the schema-factory pattern works.
- [x] Manual: `/register`, `/forgot-password`, `/reset-password`, `/verify-email` all render translated (the last three via `CinematicAuthShell`).
- [x] Manual: `/terms` renders translated title/summary/disclaimer through the Server Component `getTranslations` path.
- [x] Fixed during verification: Japanese hero headline lines overlapped — see "Typography note" above.
- [x] `2026-09-16` re-run after the branch caught up with 3 rounds of `development` merges: typecheck/lint/`test:contracts`/build all still pass clean on the merged state, so the i18n layer didn't regress against ~5 months of unrelated feature work landing in parallel.
- [x] `2026-09-16`: the topbar `compact` `LanguageSwitcher` (`src/app/(app)/layout.tsx`) initially rendered its `Globe` icon at `size={16}` inside the same 24px circle its neighbors (`ThemeToggleButton`, the help button) use for a `size={12}` icon — caught by diffing against those sibling components' source, not by looking at a screenshot, and fixed to match.
- [ ] Manual: inside a workspace (needs a running backend), confirm the sidebar nav labels translate **and** that the new topbar `compact` switcher renders and functions. Not exercised — the local backend/gateway was not running during this or the previous pass, so authenticated routes could not be loaded. These are covered by `test:i18n-catalog`, typecheck, and (for the icon sizing) direct comparison against sibling component source, but have not actually been seen on screen.
- [x] `2026-09-16` Phase B batch 1 (`home`/`tasks`/`ai-chat`/`dashboard`/`knowledge`/`voice-profiles` + their subcomponents): `typecheck`, `lint` (0 errors), `test:i18n-catalog` (23 subtests), and `test:contracts` (full chain, exit 0 — including the two `check-admin-knowledge-contract.mjs` assertions repaired for the i18n-aware pattern) all pass; production `build` succeeds.
- [ ] Manual in-browser verification of batch 1's authenticated pages was **not possible** in this environment: `warptalk-backend`'s gateway isn't running, and `src/proxy.ts` gates every `(app)/[workspaceSlug]/*` route behind an `HttpOnly` access-token cookie that can't be set from a script — the middleware always redirects to `/login` before the page renders. This is a real gap, not a formality: the layout-safety analysis for this batch (knowledge-table's badge column, the various `Workspace*Module`/`Workspace*Pill` components) was done by reading source and cross-checking against the CSS classes already verified safe in the Phase A exploration, **not** by looking at the rendered page. Re-verify visually the first time a local backend is available, starting with `[workspaceSlug]/knowledge` in `ja` (the batch's one real risk zone: the State column's badge pills).
- [x] `2026-09-24` Phase B batch 8 (live meeting room: control bar/mic-check, call chrome, chat panel, room detail page, transcript panel, summary rail) + admin Insights dashboard, done as 7 parallel agents on disjoint files/namespaces then reconciled centrally: `tsc --noEmit` clean; `eslint` on all 15 touched files — 0 errors (5 pre-existing warnings, none introduced); `node --test` on all 5 touched lib files' existing suites (`record-sharing`, `room-occupancy`, `transcript-absence`, `summary-absence`, `insights-metrics`) — 70/70 pass unmodified, confirming every optional-translator default still reproduces the original English exactly; `test:i18n-catalog` 104/104; `test:english-ui` clean; `test:contracts` full chain exit 0 (15 assertions across 12 scripts repaired centrally with the split-assertion pattern — see the batch notes above — everything else in the ~230-check suite unaffected); production `build` succeeds, 84 routes.
- [ ] Manual in-browser verification of this batch's authenticated pages (the live meeting room, the room detail page, `/admin` Insights) was **not possible** in this environment, for the same reason as batch 1: no running backend gateway, so `src/proxy.ts` always redirects to `/login`. Layout-safety for translated strings was judged by reading the exact Tailwind classes around each replaced string and confirming no dimension/wrapping class was touched — not by looking at the rendered page. Re-verify visually the first time a local backend is available; the highest-risk spots are the meeting control bar's settings menu (many short labels in a fixed-width flyout) and the admin Insights cards (dense grid, numbers plus new translated captions).
- [x] `2026-09-24` meeting-type picker + `AdminPlansPage`: `tsc --noEmit` clean; `eslint` on all 5 touched files — 0 errors; `test:i18n-catalog` 106/106 (up from 103, three new subtests for `adminBillingPlansLegacy.json`); `test:english-ui` clean; `test:contracts` full chain exit 0, including `check-room-surface-contract.mjs`'s updated assertions ("Room surface contract... PASS") and `check-admin-surface-contract.mjs`'s path checks for `billing/plans/page.tsx` (unaffected, path-based); production `build` succeeds. User's own screenshots (from their local `localhost:3000` with a working backend) were the source of both gaps — this session's environment still has no reachable backend, so the fix was verified by source/contract/build only, not by loading either page.
